# Berkeley Payments — Card Issuing API (staging)
# Copy this file to `.env` and fill in real values. NEVER commit `.env`.

# Base URL for the environment under test
BASE_URL=https://api.staging.pungle.co

# Bearer token (private API key) issued by Berkeley for staging
BP_API_KEY=replace-with-your-staging-api-key

# Program under test
PROGRAM_ID=137
